name: Run tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: johannahaffner/pycutest:v0.2
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Run your tests
        run: |
          # Your commands here
          # For example:
          python -c "import pycutest; print('pycutest is working!')"